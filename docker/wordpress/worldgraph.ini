; Local development limits. The plugin accepts story uploads up to 20 MiB,
; so PHP and multipart requests need enough headroom to reach that validator.
upload_max_filesize = 32M
post_max_size = 40M
memory_limit = 512M
max_execution_time = 300
max_input_time = 300
